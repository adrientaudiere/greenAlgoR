% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ga_targets_network.R
\name{ga_targets_network}
\alias{ga_targets_network}
\title{Visualize a \code{targets} pipeline as a resource-annotated network}
\usage{
ga_targets_network(
  store = targets::tar_config_get("store"),
  script = targets::tar_config_get("script"),
  tar_network_raw = NULL,
  size_by = "seconds",
  color_by = "bytes",
  layout = "fruchtermanreingold",
  label = TRUE,
  arrow_gap = 0.02,
  ...
)
}
\arguments{
\item{store}{(character, default \code{targets::tar_config_get("store")}) Path to
the targets data store. See \code{?targets::tar_network()}.}

\item{script}{(character, default \code{targets::tar_config_get("script")}) Path to
the target script file (e.g. \verb{_targets.R}). Required by
\code{targets::tar_network()} to build the pipeline graph. See
\code{?targets::tar_network()}.}

\item{tar_network_raw}{(optional list) A precomputed
\code{targets::tar_network()} result (a list with \verb{$vertices} and
\verb{$edges}). When supplied, \code{store} is ignored. Useful for tests, custom
analyses, or when the pipeline metadata is already loaded.}

\item{size_by}{(character, default "seconds") Vertex attribute mapped to node
size. One of "seconds" (runtime) or "bytes" (output size).}

\item{color_by}{(character, default "bytes") Vertex attribute mapped to node
color. One of "seconds", "bytes" or "status" (the target build status).}

\item{layout}{(character, default "fruchtermanreingold") A network layout
name passed to \code{ggnetwork::ggnetwork()}.}

\item{label}{(logical, default TRUE) Draw the target names next to the nodes.}

\item{arrow_gap}{(numeric, default 0.02) Gap left between the arrow head and
the target node, passed to \code{ggnetwork::ggnetwork()}.}

\item{...}{Additional arguments passed on to
\code{ggnetwork::ggnetwork()}.}
}
\value{
A \code{\link[ggplot2]{ggplot}} object showing the pipeline network.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/greenAlgoR/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Draw the dependency graph of a \code{targets} pipeline with
\code{ggnetwork}, encoding the per-target computational cost as visual
aesthetics: node \strong{size} and \strong{color} map to a resource metric (runtime
in seconds or output size in bytes) and each edge \strong{width} maps to the
runtime of its downstream target. This gives a quick, at-a-glance picture of
where a pipeline spends its time and storage, complementing the aggregated
figures of \code{\link{ga_targets}()}.

The per-target metrics (\code{seconds}, \code{bytes}) are taken directly from
\code{targets::tar_network()}, so no separate \code{tar_meta()} call is
needed.
}
\examples{
targets::tar_dir({ # tar_dir() runs code from a temp dir for CRAN.
  targets::tar_script(
    {
      list(
        targets::tar_target(raw, rnorm(1e5)),
        targets::tar_target(scaled, raw * 2),
        targets::tar_target(summ, mean(scaled))
      )
    },
    ask = FALSE
  )
  targets::tar_make(reporter = "silent")
  net <- targets::tar_network(targets_only = TRUE)
  ga_targets_network(tar_network_raw = net)
})
}
\seealso{
\code{\link{ga_targets}()}, \code{\link{ga_autometric_plot}()}
}
\author{
Adrien Taudière
}
